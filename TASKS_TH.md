# w07_lab_exam — โจทย์ข้อสอบปฏิบัติ

*English version: `TASKS_EN.md` — identical in substance.*
*(ฉบับภาษาอังกฤษอยู่ที่ `TASKS_EN.md` เนื้อหาเหมือนกันทุกประการ)*

**เวลา 2 ชั่วโมง** · ทำบน **VM ของตนเอง** จากเครื่องในห้องแลบ · เปิดหนังสือได้: repository แล็บของตนเอง,
`man` และ `--help` · ห้ามแชต ห้ามใช้ AI assistant ห้ามค้นเว็บ · ขั้นตอน clone และส่งงานอยู่ใน `README.md`

> **เริ่มต้น:** `bash init.sh`
> **ดูคะแนนเมื่อไหร่ก็ได้:** `bash check.sh`
> **ส่งงาน:** `bash submit.sh` แล้วยืนยันผลในแท็บ Actions บน GitHub

ข้อสอบมี **12 graded checks ข้อละ 1 คะแนน** · fixture ของคุณถูกสร้างจากรหัสนักศึกษาของคุณเอง
**คำตอบของแต่ละคนจึงไม่เหมือนกัน — ลอกตัวเลขจากเพื่อนได้ 0 คะแนน**

**ทั้งสี่ Part เป็นอิสระต่อกัน** ถ้าติด Part ใดให้ข้ามไปทำ Part อื่นก่อน ไม่มีข้อใดต้องใช้ผลของข้อก่อนหน้า
`check.sh` บอกแค่ **PASS / FAIL** จะไม่บอกว่าคำตอบที่ถูกคืออะไร

> **Part 2–4 มีโครงสคริปต์ (stub) ให้แล้ว** — `summary.sh`, `harden.sh`, `release.sh`
> อยู่ในโฟลเดอร์ตั้งแต่ต้น ไม่ต้องเขียนจากศูนย์ เปิดไฟล์แล้วเติมเฉพาะบล็อกที่เขียนว่า `TODO`
> ส่วนที่ให้มาแล้ว (shebang, การตรวจเบื้องต้น, บล็อกเขียนรายงาน, regex) แก้ได้ถ้าอยากแก้
> แต่ไม่จำเป็น และไม่มีคะแนนให้กับการเขียนใหม่

แนะนำการแบ่งเวลา: Part 1 · 25 นาที — Part 2 · 28 นาที — Part 3 · 27 นาที — Part 4 · 18 นาที
เผื่อไว้ 10 นาทีแรกสำหรับ clone และตั้ง repo และ **10 นาทีสุดท้ายสำหรับส่งงาน**

---

## ข้อมูลที่ใช้สอบ

`init.sh` สร้างไฟล์ **`logs/access.log`** (และ log ชุดที่สองซึ่งใช้เฉพาะตอนตรวจ)
บรรทัดส่วนใหญ่เป็น **Common Log Format** ปกติ:

```
203.0.113.42 - - [20/Jul/2026:08:00:00 +0700] "GET /index.html HTTP/1.1" 200 1893
```

เมื่อแยกฟิลด์ด้วยช่องว่าง: `$1` คือ client IP, `$7` คือ path ที่ถูกเรียก, `$9` คือ HTTP status,
`$10` คือจำนวนไบต์ · timestamp ในวงเล็บเหลี่ยมมีช่องว่างอยู่ข้างใน จึงกินฟิลด์ `$4` และ `$5`

**ในล็อกมีบรรทัดที่ผิดรูปแบบ (malformed) ปนอยู่ด้วย** — ข้อความขยะ, บรรทัดว่าง, บรรทัดที่ถูกตัดกลาง,
บรรทัดที่ไม่มี timestamp ในวงเล็บ, บรรทัดที่ status ไม่ใช่ตัวเลข · ใส่ไว้โดยตั้งใจ

> ### นิยาม — บรรทัดที่ถือว่า **valid**
> บรรทัดจะ valid ก็ต่อเมื่อ match กับ extended regular expression (ERE) นี้เท่านั้น:
>
> ```
> ^[^ ]+ [^ ]+ [^ ]+ \[[^]]+\] "[^ ]+ [^ ]+ [^ ]+" [0-9]+ [^ ]+$
> ```
>
> ทุกข้อในข้อสอบ คำว่า "request" หมายถึง **valid line เท่านั้น**
> → **`wc -l` ทั้งไฟล์ไม่ใช่คำตอบของข้อใดเลยในข้อสอบนี้**

---

## Part 1 — อ่านล็อก (4 คะแนน)

บันทึกคำตอบแต่ละข้อไว้ในโฟลเดอร์ `results/` · ช่องว่างท้ายบรรทัดไม่มีผล แต่ข้อความส่วนเกินมีผล

### 1.1 → สร้างไฟล์ `results/task1.txt`

**สิ่งที่ต้องทำ:** นับจำนวน request ที่ **valid** ทั้งหมดใน `logs/access.log`
**output ที่ต้องได้:** ตัวเลขจำนวนเดียวในไฟล์ ไม่มีข้อความอื่นปนมาด้วย (เช่น `2544`)

### 1.2 → สร้างไฟล์ `results/task2.txt`

**สิ่งที่ต้องทำ:** หา client IP ที่ไม่ซ้ำกันทั้งหมด ซึ่งปรากฏ **ในบรรทัดที่ valid**
**output ที่ต้องได้:** 1 IP ต่อ 1 บรรทัด เรียงลำดับตามที่ `sort` จัดให้
บรรทัด malformed ก็มีเลขที่ดูเหมือน IP อยู่ด้วย — ต้องไม่หลุดเข้ามาในคำตอบ

### 1.3 → สร้างไฟล์ `results/task3.txt`

**สิ่งที่ต้องทำ:** หา **3 IP ที่ยิง request มากที่สุด** จากบรรทัดที่ valid
**output ที่ต้องได้:** 3 บรรทัด เรียงจากมากไปน้อย แต่ละบรรทัดเป็นจำนวนครั้งแล้วตามด้วย IP
คือรูปแบบผลลัพธ์ของ `sort | uniq -c | sort -rn`
ตัวตรวจ normalize ช่องว่างก่อนเทียบ ดังนั้น `    900 203.0.113.42` และ `900 203.0.113.42` ผ่านทั้งคู่

### 1.4 → สร้างไฟล์ `results/task4.txt`

**สิ่งที่ต้องทำ:** ดึงบรรทัดที่ valid ซึ่งมี **status เป็น 4xx หรือ 5xx** ออกมาทั้งหมด
**output ที่ต้องได้:** บรรทัดต้นฉบับทั้งบรรทัด ไม่ดัดแปลง เรียงตามลำดับเดิมที่ปรากฏในไฟล์
ข้อนี้เทียบแบบ **byte-for-byte**

---

## Part 2 — `summary.sh` (3 คะแนน)

**สิ่งที่ต้องทำ:** เขียนเครื่องมือวิเคราะห์ log ที่รับชื่อไฟล์จาก argument
**ไฟล์:** **`summary.sh`** — **มีโครงให้แล้ว** ที่ระดับบนสุดของโฟลเดอร์ข้อสอบ
เปิดไฟล์แล้วเติมเฉพาะบล็อกที่เขียนว่า `TODO` (ส่วนที่ให้มาแล้วไม่ต้องแก้ เช่น regex กับบล็อกเขียน `report.txt`)

```
bash summary.sh <logfile>
```

**สัญญาของโปรแกรม (contract)**

| หัวข้อ | ข้อกำหนด |
|---|---|
| input | ไฟล์ log ที่จะวิเคราะห์ รับมาจาก **`$1`** เท่านั้น ห้าม hardcode ชื่อไฟล์ไว้ในโค้ด |
| output file | **`report.txt`** เขียนลงใน **current working directory** |
| เมื่อสำเร็จ | `exit 0` |
| เมื่อไฟล์ input ไม่มีอยู่หรืออ่านไม่ได้ | พิมพ์ข้อความ error, exit ด้วยค่าที่ **ไม่ใช่ 0** และ **ต้องไม่สร้าง `report.txt`** |

**`report.txt` ต้องมี 6 บรรทัดนี้** — ชื่อ label ต้องตรงตามนี้ทุกตัวอักษร ตามด้วยเครื่องหมาย `:`
เว้นวรรคอย่างน้อยหนึ่งช่อง แล้วจึงเป็นค่าที่คำนวณได้เพียงค่าเดียวจนจบบรรทัด:

```
Total requests: <จำนวนบรรทัดที่ valid>
200 responses: <บรรทัด valid ที่ status = 200>
404 responses: <บรรทัด valid ที่ status = 404>
500 responses: <บรรทัด valid ที่ status = 500>
Top IP: <IP ที่มี request มากที่สุด>
Top page: <path ใน $7 ที่ถูกเรียกมากที่สุด>
```

เขียนด้วย Bash ล้วน, Python, หรือ Bash เรียก Python ก็ได้ · ตัวตรวจจะ copy ไฟล์ `*.sh` และ `*.py`
ที่อยู่ระดับบนสุดของโฟลเดอร์ข้อสอบ เข้าไปในไดเรกทอรีชั่วคราวแล้วรัน `bash summary.sh <log>` ที่นั่น
ดังนั้น relative path ระหว่างไฟล์ของคุณเองยังใช้ได้ แต่ไฟล์อื่นในโฟลเดอร์ข้อสอบจะไม่อยู่ที่นั่น

**วิธีตรวจ**

| check | ตัวตรวจทำอะไร |
|---|---|
| `tool_arg_handling` | รันด้วยชื่อไฟล์ที่ไม่มีอยู่จริง → ต้อง exit ไม่ใช่ 0 **และ** ต้องไม่มี `report.txt` |
| `tool_accuracy_renamed` | รันกับ **สำเนาของ `logs/access.log` ที่เปลี่ยนชื่อแล้ว** ในไดเรกทอรีชั่วคราว → ค่าทั้ง 6 ต้องถูก |
| `tool_second_log` | รันกับ **log คนละชุดที่คุณยังไม่เคยวิเคราะห์** → ค่าทั้ง 6 ต้องถูก และ exit 0 |

check ตัวที่สามคือเหตุผลว่าทำไมการ hardcode คำตอบจาก Part 1 ลงไปจึงใช้ไม่ได้

---

## Part 3 — `harden.sh` (3 คะแนน)

`init.sh` จงใจทิ้ง `log-tool/` ไว้ในสภาพสิทธิ์พัง ๆ

**สิ่งที่ต้องทำ:** เขียนสคริปต์แก้ permission ของไดเรกทอรีที่รับมาให้ถูกต้องทั้งหมด
**ไฟล์:** **`harden.sh`** — **มีโครงให้แล้ว** เติมเฉพาะบล็อก `TODO` (รวมถึงเติมค่า `MODES` จากตารางข้างล่าง)

```
bash harden.sh <directory>
```

**mode ปลายทางที่ต้องได้ ครบทั้ง 9 path ในไดเรกทอรีที่รับมา**

| path | เริ่มต้นเป็น | ต้องจบที่ |
|---|---|---|
| `run.sh` | `644` | **`755`** |
| `analyze.py` | `777` | **`755`** |
| `webserver.log` | `600` | **`644`** |
| `report.txt` | `666` | **`644`** |
| `secrets/` | `755` | **`700`** |
| `secrets/deploy.env` | `666` | **`600`** |
| `secrets/api_token` | `644` | **`600`** |
| `archive/` | `744` | **`755`** |
| `archive/old.txt` | `640` | **`644`** |

**ข้อกำหนด**

1. รับ target directory จาก `$1`
2. ถ้า `$1` หายไป หรือไม่ใช่ directory: พิมพ์ error, exit ด้วยค่าที่ **ไม่ใช่ 0** และ
   **ต้องไม่เขียนไฟล์ `harden_report.txt`**
3. หลังจากรันแล้ว คำสั่ง `find <dir> -perm -002` ต้องไม่พิมพ์อะไรออกมาเลย
4. ต้อง **idempotent** — รันรอบที่สองต้อง `exit 0` และต้องไม่มี mode ใดเปลี่ยนแม้แต่ path เดียว
5. เมื่อทำงานสำเร็จ ให้เขียน **`harden_report.txt`** ลงใน current working directory ด้วย
   redirection ครั้งเดียว โดยมีอย่างน้อย: ชื่อ target, จำนวน path ที่ยัง world-writable เหลืออยู่
   และ mode ของ `secrets/`
6. ห้ามใช้ `sudo` — ทุกไฟล์เป็นของคุณอยู่แล้ว

> `chmod -R 755 log-tool/` ไม่ใช่ทางลัด — มันทำให้ไฟล์ข้อมูลทุกไฟล์กลายเป็น executable
> และเปิด `secrets/` ให้คนทั้งเครื่องอ่านได้

**วิธีตรวจ:** ตัวตรวจสร้างสำเนาที่พังขึ้นมาใหม่ในไดเรกทอรีชั่วคราว ไม่ได้ตรวจ `log-tool/` ของคุณ
รายการตรวจคือ mode ครบถูกทั้ง 9 path (`harden_modes`) · ไม่มี world-writable
(`harden_no_world_writable`) · รอบสองไม่เปลี่ยนอะไรและ argument ผิดต้อง exit ไม่ใช่ 0 โดยไม่เขียนรายงาน
(`harden_idempotent_and_args`)

---

## Part 4 — `release.sh` (2 คะแนน)

**สิ่งที่ต้องทำ:** เขียนสคริปต์ deploy ไฟล์ไปยังปลายทาง โดยรักษา mode และเวลาแก้ไขไว้ครบ
**ไฟล์:** **`release.sh`** — **มีโครงให้แล้ว** เติมเฉพาะบล็อก `TODO` (คำสั่งถ่ายโอนบรรทัดเดียว)

```
bash release.sh <destination-directory>
```

**สัญญาของโปรแกรม (contract)**

| หัวข้อ | ข้อกำหนด |
|---|---|
| ต้นทาง | `log-tool/` ใน current directory เสมอ — ไม่รับเป็น argument |
| ปลายทาง | `$1` ซึ่งเป็นไดเรกทอรีที่มีอยู่แล้ว |
| สิ่งที่ต้องไปถึงปลายทาง | **contents** ของ `log-tool/` คือ `<dest>/run.sh` **ไม่ใช่** `<dest>/log-tool/run.sh` |
| สิ่งที่ต้องไม่ไปถึงปลายทาง | `secrets/` ไม่ว่าในรูปแบบใด |
| mode และ timestamp | ต้องไปถึงโดย **ไม่เปลี่ยนแปลง** |
| เมื่อสำเร็จ | `exit 0` |

ตัวตรวจรันสคริปต์ของคุณภายใต้ **`umask 077`** บนต้นทางที่ hardened แล้วและถูกประทับเวลาไว้
จากนั้นเทียบ checksum, **mode และเวลาแก้ไข (mtime)** ของทุกไฟล์ที่ปลายทาง
**การ copy ที่ประทับเวลาใหม่ ไม่ถือเป็นการ deploy**

**วิธีตรวจ:** `release_preserves` (เทียบ checksum + mode + mtime ของ 4 ไฟล์) และ
`release_layout` (contents ไม่ซ้อนชั้น, ไม่มี `secrets/`, ไฟล์เครื่องมืออยู่ครบ)

---

## Challenge — คะแนนพิเศษ (ไม่มีผลต่อคะแนนหลัก)

ผลของ challenge บันทึกแยกไว้ใน `results/challenge_report.json` และ GitHub action คนละตัวที่เขียวเสมอ
จึงไม่มีทางดึงคะแนนหลักของคุณลง

- **A.** `release.sh --dry-run <dest>` — รายงานว่าจะคัดลอกอะไรบ้าง แต่ไม่คัดลอกจริง และ `exit 0`
- **B.** `harden.sh --audit <dir>` — รายงานว่าอะไรผิดบ้าง แต่ไม่แก้ไขอะไรเลย และ `exit 0`
- **C.** `release.sh` เขียนบรรทัดต่อท้ายไฟล์ `deploy.log` ทุกครั้งที่รัน
- **D.** `summary.sh --top-only <log>` — พิมพ์เฉพาะบรรทัด `Top IP:` และ `Top page:` เท่านั้น

---

## การส่งงาน

```bash
bash check.sh
bash submit.sh
```

จากนั้นเปิด repository ของคุณบน GitHub → แท็บ **Actions** และยืนยันว่า run ของ commit ล่าสุดขึ้นจริง

> **ผล run ใน Actions คือหลักฐานการส่งงานอย่างเป็นทางการ** เผื่อเวลาไว้สิบนาทีสำหรับขั้นตอนนี้ —
> ข้อสอบที่ทำได้เต็มแต่ไม่ได้ push ขึ้นไป มีค่าเท่ากับศูนย์
> ถ้า push ถูกปฏิเสธ ให้ดูตาราง "เมื่อมีปัญหา" ใน `README.md` และ **ห้ามสั่ง `git pull`**
